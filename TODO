Further ideas:

* Animations
  - Increment a static clock offset
  - Increase the rotation speed
  - Progressive render
* Use PNM instead of PNG and avoid cutting to eight-bit colour
* Add a full aeroplane to the scene
